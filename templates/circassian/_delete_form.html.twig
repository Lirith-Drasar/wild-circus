<form method="post" action="{{ path('circassian_delete', {'id': circassian.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ circassian.id) }}">
    <button class="btn">Delete</button>
</form>
