{% extends 'base.html.twig' %}

{% block title %}Circassian{% endblock %}

{% block body %}
    <h1>Circassian</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ circassian.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ circassian.Firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ circassian.Lastname }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('circassian_index') }}">back to list</a>

    <a href="{{ path('circassian_edit', {'id': circassian.id}) }}">edit</a>

    {{ include('circassian/_delete_form.html.twig') }}
{% endblock %}
